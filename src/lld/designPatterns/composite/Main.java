package lld.designPatterns.composite;

public class Main {
    // file system has file or directory
    // inside directory there can be files and directories
    // wherever there is a tree like structure (object inside object)
    public static void main(String[] args) {

    }
}
